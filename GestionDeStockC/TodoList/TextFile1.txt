Champs de saisie (partout) :
--verifier si saisie selon numerique ou alphanumerique
--empecher la saisie de plus de X caractere (variable selon les champs)
--verifier les champs obligatoire
--corriger faute orthographe 

--passer tout les dgv en readonly

User Liste produit :
--reprendre l'export excel

SQL Table client :
--ajouter numero client

SQL : reprendre toutes les tables et verifier les champs obligation

User Affectation :
-- supprimer ligne si qte = 0  ==> doit se faire dans table affectation SQL

FRM Detail commande :
--contextstrip modifier prend le stock dans table produit
--contexstrip modifier ne permet pas de recuperer la condition si unitaire alors qte = 1

Verifier :
--ecriture dans table affectation
--filtre DVG
--critere d'erreur
--cas de multiple utilisateur (IMPORTANT)
